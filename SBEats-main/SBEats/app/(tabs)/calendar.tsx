/**
 * Calendar tab screen for users to schedule and view planned restaurant visits.
 */
import CalendarView from '@/components/CalendarView';

export default function CalendarScreen() {
  return <CalendarView />;
}